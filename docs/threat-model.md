# Threat Model

Threats include stolen identities, compromised endpoints, malicious CI runners, unsafe production changes, privileged workloads, public network exposure and hardcoded secrets.

Controls include MFA, device posture, least-privilege workload checks, infrastructure analyzers, risk thresholds and audit logging.

The default API token is for development only.
